from . import views
from django.urls import path


urlpatterns = [
    # path('checkout/create-checkout-session/' , views.Creax`teCheckoutSession.as_view()),
    # path('webhook/' , views.WebHook.as_view()),
    # path('api/create-payment-intent/', views.CreatePaymentIntent.as_view()),
]